version: '3'
services:
  # app:
  #   build: 
  #     context: .
  #     dockerfile: dockerfile
  #   ports:
  #     -"8080:8080"
  #   depends_on:
  #     - db
  #   environment:
  #     ADDR: 0.0.0.0:8080
  #     MONGO_URI: mongodb://db:27017
  db:
    image: mongo:latest
    restart: always
    ports:
      - "27017:27017"

